import { RequestInit } from "./types";
import { Response } from "node-fetch";
export declare function setHeader(options: RequestInit, key: string, value: string): void;
export declare function isCompressed(init: RequestInit, response: Response): boolean;
